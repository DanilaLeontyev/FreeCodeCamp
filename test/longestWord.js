import assert from 'assert';
import {longestWord} from '../src/task/longestWord';


//тут пишем тесты
describe('longestWord', function () {
    it('should return true', function () {
        assert.equal(longestWord("eye"), true)
    });
});
